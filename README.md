# Persönliche Website

Quelle: `body.html` (Vorlage mit Bildplatzhaltern) und `img/`.
Bauen: `python3 build.py` erzeugt daraus die fertige `index.html` mit eingebetteten Bildern.

Veröffentlicht über GitHub Pages.
